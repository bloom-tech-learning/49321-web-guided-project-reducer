export const initialState = {
    title: "Hello Earthling",
    editing: false,
    newTitleText: ""
}

//{type: "TOGGLE_EDITING"}
const reducer = (state, action) => {
    
    return(state);
}

export default reducer;